# Python Directory Search Wrapper

A simple wrapper to perform a case-insensitive directory search.

## Example usage:

```python

path_list = dirsearch(base_folder)

```

## Optional parameters

*extension*='\*.\*'
*recursive*=True
